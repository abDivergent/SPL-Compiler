proc username
{
    return;
},
proc username
{
    do
    {
        if(condition[me])
        then
        {
            var := false;
        };
    }
    until( not( or( true, 5 ) ) );
    return;
    arr bool[5] mybool;
    string mystring;
},
main
{
    myfield[5] := myexpr;
    call mycall;
    halt;
    string mystring;
}